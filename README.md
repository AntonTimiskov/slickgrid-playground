slickgrid-playground
====================

install dependencies:

git submodule init
git submodule update

npm install
bower install

run static server to serve files (3000 port):

node ./node_modules/statik/bin/statik --verbose .
